<?xml version="1.0" ?>

<project name="MediaLib - Properties">

	<property name="root.dir" value="${basedir}"/>
	<property name="lib.dir" value="${root.dir}/lib"/>
	<property name="build.dir" value="${root.dir}/tmp"/>
	<property name="classes.dir" value="${build.dir}/classes"/>
	<property name="utils.builds.dir" value="${root.dir}/../utils/builds"/>

	<patternset id="resource.pattern">
		<include name="**/*.jpg"/>
		<include name="**/*.jpeg"/>
		<include name="**/*.png"/>
		<include name="**/*.gif"/>
		<include name="**/*.xml"/>
		<include name="**/*.properties"/>
		<include name="**/*.odtt"/>
		<include name="**/*.dtd"/>
	</patternset>

</project>
